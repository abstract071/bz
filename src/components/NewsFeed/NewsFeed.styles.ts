import { StyleSheet } from 'react-native'


const styles = StyleSheet.create( {
  title: {
    fontSize: 20,
    fontWeight: 'bold',
    paddingBottom: 12
  },
  list: {
    maxHeight: 400
  }
} )

export default styles
